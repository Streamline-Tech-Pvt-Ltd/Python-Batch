# Interview Questions & Answer

1. What is python ?
Ans:.Python is a simple,high level,dynamic,interpreted and general purpose programming language.
     1)Simple:Python is easy to learn and write.
     2)High level:Python uses human-readable words, so programmers don't need
       to manage complex computer hardware details.
     3)Dynamic:You don't need to specify the data type of a variable. 
     4)Interpreted:Python executes code line by line instead of converting
       the entire program into machine code before running.
     5)General purpose:Python can be used for many different types of applications.

2. Explain interpreted in detail ?
Ans:.Python is an interpreted language because its source code is executed line by line
     by the Python interpreter without requiring separate compilation.This makes debugging 
     easier and program development faster.

3. Explain indentation in python ?
Ans:.Indentation is the use of spaces or tabs at the beginning of a line to define a 
     block of code in Python.It is mandatory and helps improve code readability and structure.

4. Difference between static & dynamic ?
Ans:.| Static                                                   |             Dynamic                                 
| ------------------------------------------------------------- | --------------------------------------------------- |
| Variable type is declared before use.                         | Variable type is decided automatically at runtime.  |
| Data type cannot change easily.                               | Data type can change during execution.              |
| Errors related to types are found beforerunning the program.  | Type errors are found while the program is running. |
| More strict.                                                  | More flexible.                                      |
| Examples: C, C++, Java                                        | Examples: Python, JavaScript                        |

5. What is IDE & Give me three example ?
Ans:.IDE (Integrated Development Environment) is software that provides tools such as a code editor,
     compiler/interpreter, and debugger in a single environment for developing programs.
      Examples;PyCharm, Visual Studio Code,Jupyter notebook and Spyder.

6. Explain the types of python language ?
Ans:.1. Sequential Programming
        In sequential programming, statements are executed one after another in order 
        from top to bottom.
     2. Functional Programming
        In functional programming the program is divided into functions. Each function performs 
        a specific task and can be called whenever needed.
     3. Object-Oriented Programming (OOP)
        In OOP programs are organized using classes and objects Data and functions
         are combined into a single unit called an object.


 # Variables
 7. What is varibles in Python ?
 Ans:.A variable is a name used to store data (values) in memory.
    EX:name = "Urmila"
       age = 20
       salary = 25000.50
       
 8. What are the rules for naming variables ?
 Ans:. Rule 1:Variable name must start with a letter (A-Z, a-z) or underscore (_)
     EX: name = "Urmila"
         print(name)

         _name="Urmila"
         print(_name)

   * Rule 2:Variable name cannot start with a number
      Ex: Invalid
      2Name = "Urmila" 

     #Valid
     name2 = "Urmila"
     print(name2)

  * Rule 3:Special characters are not allowed
      Ex: Invalid     
         student@name = "Urmila"
      
  * Rule 4:Variable names are case-sensitive
     name = "Urmila"
     Name = "Said"
     print(Name) # Said
     print(name) # Urmila

  * Rule 5:Spaces are not allowed
     Ex:Invalid
      my name = "Urmila"
      print(my name)

    # Correct
     my_name = "Urmila"
     print(my_name)

 * Rule 6:Keywords cannot be used as variable names
     Ex:Invalid
      if = "Urmila"
      print(if)

   # Correct
    student_name = "Urmila" 
    print(student_name)

9. What is a naming convention in Python?
Ans:. Naming conventions are rules for writing variable, function, class, and constant names in a readable format.
    * Naming Conventions Four Cases

   1.snake_case (recommended variables & function)
   All letters are lowercase & Words are separated by underscores (_).
   student_name = "Urmila"

   2.camelCase 
   First word starts with a lowercase letter & Next word start with uppercase letter. 
   studentName = "Urmila"

    3.PascalCase (use for classes)
    Every word starts with a capital letter.
    class StudentDetails:

   4.UPPER_CASE (used for constants)
    All letters are capital & Words are separated by underscores.
    MAX_SIZE = 100

10. What are keywords in Python ?
Ans:.Keywords are reserved words in Python that have special meanings and cannot be used as variable names, function names, or identifiers.
  * Keywords
   Ex: if, else, for, while, True, False, class, def, return.

  # Syntax
   import keyword 
   print(keyword.kwlist)

 11.What is an f-string ?
 Ans:.An f-string (formatted string) is used to insert 
      variables or expressions directly inside a string using {} brackets.
      *Syntax:f"string {variable}"
      EX:name = "Urmila"
         age = 20
         print(f"My name is {name} and I am {age} years old.")

 12.What is input function ?
 Ans:.The input() function is used to take input from the user through the keyboard.
    Ex:name = input("Enter your name: ")
       print("Hello", name)

13.What is type Casting ?
Ans:Type Casting means converting one data type into another data type.

14.Difference between input() and print()
Ans:
|input()                                       | print()                               |
| -------------------------------------------- | ------------------------------------- |
| Used to take input from the user.            | Used to display output on the screen. |
| Returns data as a string (`str`) by default. | Displays any type of data.            |
| Waits for the user to enter a value.         | Does not wait for user input.         |
| Used for accepting data.                     | Used for showing data.                |

15.Difference Between Assignment and comparison operator.
Ans:
    = (Assignment Operator)	                   == (Comparison Operator)
  Used to assign a value to a variable.     	    Used to compare two values.
  Stores data in a variable.	                   Returns True or False.
  Does not perform comparison.	                Checks whether values are equal.


# Data types

16.What is a data type in a python ?
Ans:Data types are used to store different types of  data in a variable.

17.What are the built-in data types in Python?
Ans:Numeric → int, float, complex
Text → str
Sequence → list, tuple, range
Set → set, frozenset
Mapping → dict
Boolean → bool
Binary → bytes, bytearray, memoryview
None Type → NoneType

18.What is an Integer (int) ?
Ans:.Stores whole numbers (without decimal points).
    Ex:age = 20
       marks = 95

19.What is a Float (float) ?
Ans:.Stores decimal numbers.
    Ex:.price = 99.99
        height = 5.8

20.What is a Complex Data Type ?
Ans:Stores numbers with a real and imaginary part.
   Ex:.num = 2 + 3j

21.What is a String (str) ?
Ans:.A string is a collection of characters enclosed in quotes.
    Ex:name = "Urmila"
       city = 'Pune'

22.What is a List?
Ans:.An ordered and mutable collection.
     Ex:.numbers = [10, 20, 30, 40]

23.What is a Tuple ?
Ans:.An ordered but immutable collection.
    Features:
    * Ordered
    * Immutable (cannot be changed)
    * Allows duplicates
    Ex:.data = (10, 20, 30)
  
24.What is set() ?
Ans:.An unordered collection of unique values.
    Ex: nums = {10, 20, 30, 20}
        print(nums)  

25.What is boolean(bool) ?
Ans:.Stores only two values:
     True
     False 
     Ex:is_student = True
        is_married = False

26.What is ditionary(dict) ?
Ans:.Stores data in key-value pairs.
    Ex:student = {
    "name": "Urmila",
    "age": 20
    }

27.What is Range (range) ?
Ans:.Generates a sequence of numbers.
   Ex:.r = range(1, 6)
      for i in r:
          print(i)    

28.Difference between List and Tuple ?
Ans:
| List                                        | Tuple                          |
| ------------------------------------------- | ------------------------------ |
| Uses `[]`                                   | Uses `()`                      |
| Mutable (can change)                        | Immutable (cannot change)      |
| Supports `append()`, `remove()`, `extend()` | Does not support these methods |
| Uses more memory                            | Uses less memory               |
| Slower than tuple                           | Faster than list               |
| Used for dynamic data                       | Used for fixed data            |

29.What is indexing in python ?
Ans:Indexing is used to access individual elements (characters or items) from a sequence
    such as a string, list, tuple, etc., using their position number called an index.

    * Positive Indexing: Positive indexing starts from 0 and moves from left to right.
      Ex:name = "Python"
        print(name[0])  # P
        print(name[1])  # y
        print(name[2])  # t

    * Negative Indexing: Negative indexing starts from -1 and moves from right to left.
      Ex:name = "Python"
         print(name[-1])  # n
         print(name[-2])  # o
         print(name[-3])  # h

30.What is slicing in Python ?
Ans:Slicing is used to access a range of multiple elements from a string, list, or tuple.
    * sequence[start : stop : step]

    start → Starting index (included)
    stop → Ending index (Index it will stop before that number)
    step → Increment/decrement value (Number of position to move)
    Ex:numbers = [10, 20, 30, 40, 50]
       print(numbers[1:4])

31.What is the difference between indexing and slicing ?
Ans:
| Indexing                                  | Slicing                                         |
| ----------------------------------------- | ----------------------------------------------- |
| Used to access **one element** at a time. | Used to access **multiple elements** (a range). |
| Returns a single value.                   | Returns a sequence (string, list, tuple, etc.). |
| Uses one index position.                  | Uses start, stop, and step values.              |
| Syntax: `sequence[index]`                 | Syntax: `sequence[start:stop:step]`             |

32.What does [::-1] do in Python ?
Ans:reverses a sequence (string, list, or tuple) by traversing it from the end to the beginning using a step value of -1.

33.Difference between append() and extend() ?
Ans:
| Feature     |  append()                                  |  extend()                                     |
| ----------- | ------------------------------------------ | --------------------------------------------- |
| Purpose     | Adds a single element at the end of a list | Adds multiple elements from another iterable  |
| Argument    | Takes one item                             | Takes an iterable (list, tuple, string, etc.) |
| List Length | Increases by 1                             | Increases by the number of elements added     |
| Output      | Element is added as a single item          | Elements are added individually               |

33.Difference between remove() and POP() ?
Ans:
| Feature          |  remove()                                 |  pop()`                                       |
| ---------------- | ----------------------------------------- | --------------------------------------------- |
| Removes by       | **Value**                                 | **Index**                                     |
| Return Value     | Does not return the removed item (`None`) | Returns the removed item                      |
| Syntax           | `list.remove(value)`                      | `list.pop(index)`                             |
| Default Behavior | Must specify the value                    | Removes the last element if no index is given |
| Error            | `ValueError` if value not found           | `IndexError` if index is out of range         |

34. Why are Sets used in Python ?
Answer:Sets are used to store unique values and perform mathematical operations such as union,
       intersection, and difference efficiently.

35.Difference between list() and set() ?
Ans:
| Feature          | List                        | Set                                  |
| ---------------- | --------------------------- | ------------------------------------ |
| Definition       | Ordered collection of items | Unordered collection of unique items |
| Syntax           | `[]`                        | `{}`                                 |
| Duplicate Values | Allowed                     | Not Allowed                          |
| Indexing         | Supported                   | Not Supported                        |
| Mutable          | Yes                         | Yes                                  |
| Order            | Maintains insertion order   | Does not guarantee order             |
| Use Case         | Store ordered data          | Store unique data                    |

36.difference between add() and update() ?
Ans:
| Feature                  | add()                 | update()                                  |
| ------------------------ | --------------------- | ----------------------------------------- |
| Purpose                  | Adds a single element | Adds multiple elements                    |
| Argument                 | One element           | Iterable (list, tuple, set, string, etc.) |
| Number of Elements Added | One                   | One or more                               |
| Return Value             | `None`                | `None`                                    |

36.What is dictionary in python and their characteristic ?
Ans:A Dictionary is a built-in data type in Python used to store data in key-value pairs.
   Each key is unique.
   Values can be of any data type.
   Dictionaries are mutable (can be modified after creation).
   Dictionaries are written using curly braces {}.

  # characteristic
  1. Stores Data in Key-Value Pairs
  2. Keys Must Be Unique
  3. Values Can Be Duplicated
  4. Mutable

38.Difference between List and dictionary ?
Ans:
| Feature          | List                        | Dictionary                          |
| ---------------- | --------------------------- | ----------------------------------- |
| Definition       | Ordered collection of items | Collection of key-value pairs       |
| Syntax           | `[]`                        | `{}`                                |
| Access           | By index                    | By key                              |
| Duplicate Values | Allowed                     | Values allowed, keys must be unique |
| Order            | Ordered                     | Maintains insertion order           |
| Data Storage     | Stores only values          | Stores key-value pairs              |
| Example          | `[10, 20, 30]`              | `{"a":10, "b":20}`                  |

39.What are the characteristics of a Set ?
Ans:Elements do not have a fixed position.
   Indexing and slicing are not supported
   Duplicate values are automatically removed.
   You can add or remove elements after creation.
   A set can contain different data types.
   Since sets are unordered, elements cannot be accessed by index.

40.What is a Frozen Set?
Ans:A Frozen Set is an immutable version of a Set. Once created, it cannot be modified.
   fs = frozenset([1, 2, 3])

41.Explain set operation in python ?
Ans:Supports Set Operations
    Union (|)
    Intersection (&)
    Difference (-)
    Symmetric Difference (^)

42.What are the characteristics of a dictionary ?
Ans:1. Key-Value Pair - Data stored as key:value
    2. Mutable - Can be modified
    3. Keys Must Be Unique - Duplicate keys not allowed
    4. Values Can Be Duplicated - Allowed
    5. Accessed Using Keys - Access values using keys
    6. Heterogeneous Data Allowed - Different data types allowed
    7. Dynamic Size - Elements can be added or removed at runtime.
    
43.Difference Between set and Frozen set.
Ans:
 | Set                                | Frozen Set                      |
 | ---------------------------------- | ------------------------------- |
 | Mutable (can be changed)           | Immutable (cannot be changed)   |
 | Created using `set()`              | Created using `frozenset()`     |
 | Supports `add()` method            | Does not support `add()`        |
 | Supports `remove()` method         | Does not support `remove()`     |
 | Supports `update()` method         | Does not support `update()`     |
 | Not hashable                       | Hashable                        |
 | Cannot be used as a dictionary key | Can be used as a dictionary key |
 | Example: `{1, 2, 3}`               | Example: `frozenset({1, 2, 3})` |

44. What is an operator in Python?
Ans:An operator is a symbol that performs an operation on one
     or more operands (values or variables).

45.What are the types of operators in Python?
Ans:1.Arithmetic Operators
    2.Assignment Operators
    3.Comparison (Relational) Operators
    4.Logical Operators
    5.Bitwise Operators
    6.Membership Operators
    7.Identity Operators

46.What is the difference between = and == ?
Ans:
| `=`                 | `==`                |
| ------------------- | ------------------- |
| Assignment operator | Comparison operator |
| Assigns value       | Compares values     |
| `a = 10`            | `a == 10`           |

47.What are Arithmetic Operators ?
Ans:Used for mathematical calculations.
    +   Addition
    -   Subtraction
    *   Multiplication
    /   Division
    %   Modulus
    **  Exponentiation
    //  Floor Division

48.What is the difference between / and // ?
Ans: / returns a float value, while // returns the integer quotient.
   Ex:print(10 / 3)   # 3.3333
      print(10 // 3)  # 3

49.What are Logical Operators ?
Ans:and - Returns True if both conditions are True
    or - Returns True if at least one condition is True
    not - Reverses the result

50.What are Membership Operators ?
Ans:Used to check whether a value exists in a sequence.
   in
   not in

51.What are Identity Operators?
Ans:Used to compare object identities.
    is
    is not

52.What is the highest precedence operator in Python ?
Ans:() (Parentheses) have the highest precedence because expressions
     inside parentheses are evaluated first.
      
53.What is comparison Operator ?
Ans:Comparison Operators (Relational Operators) are used to compare two values or variables. 
    The result of a comparison is always either True or False.

54.What are Bitwise Operators?
Ans:Bitwise operators perform operations on the binary representation (bits) of integers.
    Operators:&, |, ^, ~, <<, >>

55.Difference Between and n & ?
Ans:
| `and`                                      | `&`                                       |
| ------------------------------------------ | ----------------------------------------- |
| Logical Operator                           | Bitwise Operator                          |
| Used with Boolean values (`True`, `False`) | Used with binary (bit) values of integers |
| Returns `True` if both conditions are true | Performs bit-by-bit AND operation         |
| Used in conditional statements             | Used in bit manipulation                  |

56.What is loop in python ?
Ans:Loop is used to repeat a block of code until this given condition is true.
    Two Loop: 1. For Loop 
              2. While loop

57.Difference Between For Loop And while Loop ?
Ans:
| `for` Loop                                  | `while` Loop                                      |
| ------------------------------------------- | ------------------------------------------------- |
| Used when we know how many times to repeat. | Used when we don't know how many times to repeat. |
| Easy to write.                              | Needs a condition.                                |
| Iterates over a sequence.                   | Runs based on a condition.                        |

EX:for i in range(1, 6):
    print(i)

i = 1
while i <= 5:
    print(i)
    i = i + 1

58. What is the purpose of the range() function?
Ans:range() is a built-in Python function that generates a sequence of numbers and
    is commonly used with for loops.
    EX:for i in range(1, 6):
       print(i)              #1 2 3 4 5

59. What is a conditional statement?
Ans: A conditional statement is used to make decisions in a program based on a condition.

60.What is the purpose of the if statement ?
Ans: The if statement executes a block of code only when the condition is True.

61.What is the difference between if and if-else ?
Ans:if → Executes code when the condition is true.
    if-else → Executes one block if the condition is true and another block if it is false.
    
62.What is the use of elif ?
Ans: elif is used to check multiple conditions.

63.What is a nested if statement?
Ans: An if statement inside another if statement is called a nested if.

64. Program to check Odd or Even
Ans: 
num = int(input("Enter a number: "))

if num % 2 == 0:
    print("Even")
else:
    print("Odd")

65.Take a number n from the user and print the first n terms of the Fibonacci series using a while loop.
# For Loop
n = int(input("Enter the no n:"))
a = 0
b = 1
for i in range(n):
     print(a)
     c = a + b
     a = b
     b = c


# While Loop
n = int(input("Enter the no n:"))
a = 0
b = 1
count = 0
while count < n:
     print(a)
     c = a + b
     a = b
     b = c
     count += 1

66.# 7. Take a number from the user and check whether it is a palindrome or not.
# While Loop
num = int(input("Enter a number:"))
temp = num
reverse = 0
while (temp > 0):
      remainder = temp % 10
      reverse = (reverse * 10) + remainder
      temp = temp // 10

if (num == reverse):
      print("Palindrome Number")
else:
      print("Not Palindrome number")


67. Difference between Nested if and elif
Ans: 
| Nested `if`                   | `elif`                          |
| ----------------------------- | ------------------------------- |
| `if` inside another `if`      | Checks multiple conditions      |
| Used for dependent conditions | Used for alternative conditions |


68.What is a control statement ?
Ans:A control statement controls the flow of execution of a program.
    It decides which statement will execute and how many times.
       
69.What is the purpose of the break statement?
Ans: break immediately terminates the loop.

70. What is the purpose of the continue statement?
Ans: continue skips the current iteration and moves to the next iteration.

71.What is the purpose of the pass statement?
Ans: pass is a null statement. It does nothing and is used as a placeholder

72. What are the types of control statements in Python?
Ans:
Conditional Statements (if, if-else, elif)
Looping Statements (for, while)
Jump Statements (break, continue, pass)

73.What is a function in Python ?
Ans:A function is a block of reusable code that performs a specific task

74.Why do we use functions?
Ans:Code reusability
    Reduces code duplication
    Makes code easier to understand and maintain

75.Difference Between Parameter And Argument ?
Ans:
| Parameter                                                      | Argument                                                                  |
| -------------------------------------------------------------- | ------------------------------------------------------------------------- |
| A parameter is a variable defined in the function declaration. | An argument is the actual value passed to the function when it is called. |
| Parameters receive values from arguments.                      | Arguments provide values to parameters.                                   |
| Used in function definition.                                   | Used in function call.                                                    |
|Ex: a,b,c etc                                                   |Ex: 1,2,3 etc


76.Difference Between Print() & return function ?
Ans:
| `print()`                           | `return`                                      |
| ----------------------------------- | --------------------------------------------- |
| Displays output on the screen.      | Sends a value back from the function.         |
| Output cannot be stored directly.   | Returned value can be stored in a variable.   |
| Used for showing results.           | Used for further calculations and processing. |
| Function continues after `print()`. | Function ends when `return` is executed.      |

77.Difference Between User-Defined Function and Built-in Function
Ans:
| User-Defined Function                                   | Built-in Function                      |
| ------------------------------------------------------- | -------------------------------------- |
| Created by the programmer.                              | Already provided by Python.            |
| Defined using the `def` keyword.                        | No need to define them.                |
| Used for specific tasks according to user requirements. | Used for common tasks.                 |
| Example: `add()`, `factorial()`                         | Example: `print()`, `len()`, `input()` |

78.What are function arguments in Python?
Ans:Arguments are the values passed to a function when it is called.

79.How many types of arguments are there in Python?
Ans:There are 4 types:
    Positional Arguments
    Keyword Arguments
    Default Arguments
    Variable-length Arguments (*args, **kwargs)

80.What are keyword arguments?
Ans:Arguments passed using parameter names.

81.What are default arguments?
Ans:Arguments with predefined values.

82.What are variable-length arguments?
Ans:Used when the number of arguments is unknown.
Using *args

83.What does *args mean?
Ans: *args collects multiple positional arguments into a tuple.

84.What does **kwargs mean?
Ans: **kwargs collects keyword arguments into a dictionary.

85.What is a required argument?
Ans: A parameter that must receive a value.

86.Which should come first: positional or keyword arguments?
Ans: Positional arguments must come before keyword arguments.

87.What is the advantage of using arguments?
Ans: Makes functions reusable.
    Avoids repeating code.
    Allows different input values.
    Improves flexibility and readability.

88. Difference Between *args and **kwargs in Python
Ans:,
| `*args`                                                | `**kwargs`                                          |
| ------------------------------------------------------ | --------------------------------------------------- |
| Stands for **arguments**.                              | Stands for **keyword arguments**.                   |
| Accepts a variable number of **positional arguments**. | Accepts a variable number of **keyword arguments**. |
| Stores values in a **tuple**.                          | Stores values in a **dictionary**.                  |
| Access values using indexes or loops.                  | Access values using keys or loops.                  |
| Syntax: `*args`                                        | Syntax: `**kwargs`                                  |


89. What is a Higher Order Function?
Ans: A Higher Order Function is a function that:
     Takes another function as an argument, or
     Returns another function as a result.

90. Why are Higher Order Functions used?
Ans:To reduce code duplication.
    To make code reusable.
    To improve readability.
    To implement functional programming concepts.

91. Give examples of built-in Higher Order Functions in Python.
Ans: map()
    filter()
    reduce()
    sorted()  (import the functools module)

92. Is lambda a Higher-Order Function?
Ans: No. 
   A lambda is an anonymous function, not a higher-order function. It is often passed to higher-order functions such as map(), filter(), and sorted().

93. What is the difference between a normal function and a Higher Order Function?
Ans:
| Normal Function                    | Higher Order Function             |
| ---------------------------------- | --------------------------------- |
| Performs a task                    | Takes or returns another function |
| Doesn't require function arguments | Accepts functions as arguments    |

94. What is Type Casting in Python?
Ans: Type casting is the process of converting one data type into another data type.
For example, converting:
Integer (int) → Float (float)
Float (float) → Integer (int)
String (str) → Integer (int)

95. What are the types of Type Casting?
Ans: There are 2 types:
    1.Implicit Type Casting
    2.Explicit Type Casting

96.What is Implicit Type Casting?
Ans: Python automatically converts one data type into another.

97. What is Explicit Type Casting?
Ans: The programmer manually converts one data type into another using functions like int(), float(), etc.

98. Why do we use Type Casting?
Ans:
To convert one data type into another.
To perform mathematical operations.
To avoid type errors.
To process user input correctly.

99. What is the difference between Implicit and Explicit Type Casting?
Ans: 
| Implicit Type Casting              | Explicit Type Casting                                 |
| ---------------------------------- | ----------------------------------------------------- |
| Done automatically by Python       | Done manually by the programmer                       |
| No conversion function is required | Uses functions like `int()`, `float()`, `str()`, etc. |
| Safe and automatic                 | Programmer controls the conversion                    |

100. Why is user input often type cast?
Ans: The input() function always returns a string. 
     If you want to perform calculations, you must convert it to a numeric type.

101. What is Variable Scope?
Ans: Variable scope refers to the area of a program where a variable can be accessed.

102. What are the types of variable scope in Python?
Ans: There are 2 types of variable scope:
     1.Local Scope
     2.Global Scope

103. What is a Local Variable?
Ans: A variable declared inside a function is called a local variable. It can only be used inside that function.

104. What is a Global Variable?
Ans: A variable declared outside all functions is called a global variable.
    It can be accessed throughout the program.

105. What is the difference between Local and Global Variables?
Ans: 
| Local Variable                        | Global Variable                   |
| ------------------------------------- | --------------------------------- |
| Declared inside a function            | Declared outside a function       |
| Accessible only inside the function   | Accessible throughout the program |
| Exists only during function execution | Exists until the program ends     |

106. What is the global keyword?
Ans: The global keyword is used to modify a global variable inside a function.


-------------------------------------------------------------------------------------------------------------------
 ## OOPS 

107. What is OOP?
Ans: OOP (Object-Oriented Programming) is a programming approach in which the program is organized using objects and classes.

108. What is POP?
Ans: POP (Procedure-Oriented Programming) is a programming approach in which the program is divided into functions (procedures).

109. What is the difference between POP and OOP?
Ans:
| POP                            | OOP                         |
| ------------------------------ | --------------------------- |
| Procedure-Oriented Programming | Object-Oriented Programming |
| Focuses on functions           | Focuses on objects          |
| Data is less secure            | Data is more secure         |
| No inheritance                 | Supports inheritance        |
| No polymorphism                | Supports polymorphism       |
| Suitable for small programs    | Suitable for large programs |

110. What is a Class?
Ans: A class is a blueprint or template used to create objects.

111. What is an Object?
Ans: An object is an instance of a class.

112. What are the four pillars of OOP?
Ans: Encapsulation
     Inheritance
     Polymorphism
     Abstraction

113. Difference Between Function and Method (Python)
Ans:
| Function                                                     | Method                                                    |
| ------------------------------------------------------------ | --------------------------------------------------------- |
| A function is a block of code that performs a specific task. | A method is a function that belongs to a class or object. |
| It is defined using the `def` keyword.                       | It is also defined using `def`, but inside a class.       |
| It can be called directly by its name.                       | It is called using an object or class.                    |
| It is independent of any object.                             | It is associated with an object or class.                 |
| Example: `print()`, `len()`                                  | Example: `list.append()`, `str.upper()`                   |

114. What is a Constructor?
Ans: A constructor is a special method that is automatically called when an object is created.
     It is used to initialize the object's data.

115. What is the name of the constructor in Python?
Ans: The constructor in Python is __init__().

116. Why do we use a constructor?
Ans: To initialize object data.
     To assign values to object variables.
     To perform setup tasks automatically when an object is created

117. When is a constructor called?
Ans: A constructor is called automatically when an object is created.

118. What is a Default Constructor?
Ans: A default constructor does not take any user-defined parameters (except self).

119. What is a Parameterized Constructor?
Ans: A parameterized constructor accepts values during object creation.

120. What is the purpose of self in a constructor?
Ans: self refers to the current object of the class.
    It is used to access the object's variables and methods.

121. Can we create an object without a constructor?
Ans:Yes.
     If you do not define a constructor, Python provides a default constructor automatically.

122. What happens if we define our own constructor?
Ans: Python uses the constructor defined by the programmer instead of the automatically provided one.

